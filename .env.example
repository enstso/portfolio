# Server-only values. Never expose these through a public environment prefix.
DATABASE_URL="postgresql://portfolio_user:replace_me@localhost:5432/portfolio?schema=public"
JWT_SECRET_KEY="replace-with-a-long-random-secret"

# Optional. Public rendering does not require GitHub access; this is used only
# by the authenticated admin synchronization operation.
GITHUB_TOKEN="replace-with-a-fine-grained-token-if-sync-is-enabled"
